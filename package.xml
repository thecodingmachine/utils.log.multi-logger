<?xml version="1.0" encoding="UTF-8"?>
<package>
	<displayName>Multi Logger</displayName>
	<shortDescription>Use this package if you want a logger that logs to several places at the same time.
	Using a MultiLogger, you can append several loggers. For instance, you might want to write in the
	error log using the ErrorLogLogger and to send a mail to the admin using the MailLogger...</shortDescription>
	<docUrl></docUrl>
	<logo>logo.png</logo>
	<requires>
		<require>MultiLogger.php</require>
	</requires>
	<dependencies>
		<dependency>
			<group>utils/log</group>
			<name>log_interface</name>
			<version>>=1.1</version>
		</dependency>
	</dependencies>
</package>